name: Continuous Integration

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - uses: docker://adrienbrignon/epitech-c:latest
    - name: make
      run: make
    - name: make test
      run: make test
